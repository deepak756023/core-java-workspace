import java.util.Arrays;

public class Sort {
    public static void main(String[] args){
        int[] numbers = {10, 49, 95, 37, 98};
        Arrays.sort(numbers);
        System.out.println(Arrays.toString(numbers));
    }
}
